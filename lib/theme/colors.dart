import 'package:flutter/material.dart';

const sColor = Color.fromARGB(82, 1, 32, 78);

const pColor = Color.fromARGB(255, 1, 32, 78);

const bColor = Color.fromARGB(25, 1, 32, 78);

const black = Color(0XFF000000);

const white = Color(0XFFFFFFFF);
